// HTML Parser Example
console.log( document.title ); // Outputs the title parsed from HTML

// DOM Example
document.body.style.backgroundColor = "lightblue"; // Changes background color of the page

// BOM Example
console.log( navigator.language ); // Logs the browser's language setting

// Window Object Example
window.setTimeout( () => alert( "Hello!" ), 1000 ); // Shows alert after 1 second
